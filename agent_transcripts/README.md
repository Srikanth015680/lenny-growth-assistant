# Agent transcripts

Real records of the implementation work performed to build this project,
written as it happened — not reconstructed afterward. Per the build spec's
own instruction, nothing here is fabricated: every bug, fix, and
verification step listed actually occurred in the build sandbox. Secrets
were never present in this environment (no real API keys were used), so
there's nothing to strip.

Read `09_testing_and_debugging.md` first if you want the fastest picture
of what's actually verified vs. what's code-complete-but-unverified — it's
also summarized in `docs/troubleshooting.md` and the README's "Known
limitations."
